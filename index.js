//  whenever use node then you have to package.json file 
// npm init to create the package.json (--yes )

//add node package
  // npm install/i packagename or ...


  var _=require('underscore');
//assumes
  // core module/ file or folder
  // node_modules folder

const res=_.contains([1,2,3,4,],4);

console.log(res)
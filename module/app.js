//  avoid variables and functions defined in global , for tha we can use modularity . create smaller module of variables and function,
// every file in node consider as module.


// main module 
// console.log(module ); scope to that module

const logg=require('./createLoadModule');

logg('vikash')